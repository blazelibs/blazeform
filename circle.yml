machine:
  services:
    - docker

dependencies:
  override:
    # Need to override the dependencies or ciricle will do something bad, ask Nick what that is,
    # he couldn't remember.  :)
    - pwd

test:
  override:
    - ls -la
    - /home/ubuntu/$CIRCLE_PROJECT_REPONAME/docker-run-tests

deployment:
  codecov:
    branch: /.*/
    commands:
      - bash <(curl -s https://codecov.io/bash) -t  "ee3b6d99-0843-4941-b90f-8b507ff956b4"

